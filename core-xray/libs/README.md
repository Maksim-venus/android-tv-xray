# libv2ray.aar (not committed)

Download with:

```bash
./scripts/fetch-libv2ray.sh
```

Pinned release: **AndroidLibXrayLite v26.9.9**
https://github.com/2dust/AndroidLibXrayLite/releases/tag/v26.9.9

`assemble*` depends on this file. Gradle runs the fetch script automatically.
